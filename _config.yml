name: "Modena Interiors"
description: "Interior Refurbishment Solutions"
exclude: ["Gemfile", "Gemfile.lock", "Gemfile.lock", "assets", "README.md"]
timezone: Europe/London

sass:
  sass_dir: "assets/stylesheets" 

contact_number: "0772 8032 929"

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "default"
